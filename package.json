{
  "name": "docker-web-app",
  "version": "1.0.0",
  "description": "A basic server with REST API's to support CRUD operations.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "author": "Sandeep Reddy",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.5.3"
  }
}
